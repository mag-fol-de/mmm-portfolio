\documentclass[a4paper,11pt]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{geometry}
\geometry{margin=2.2cm}
\usepackage{xcolor}
\usepackage{graphicx}
\graphicspath{{figures/}}
\usepackage{tikz}
\usetikzlibrary{calc}
\usepackage{everypage}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{caption}
\usepackage{float}
\usepackage[colorlinks=true,linkcolor=blue,urlcolor=blue]{hyperref}

\setlength{\parskip}{0.6em}
\setlength{\parindent}{0pt}

\definecolor{bordercolor}{RGB}{68,94,114}

\AddEverypageHook{%
  \begin{tikzpicture}[remember picture, overlay]
    \draw[line width=4pt, color=bordercolor]
      ($(current page.south west) + (1cm,1cm)$) rectangle
      ($(current page.north east) - (1cm,1cm)$);
  \end{tikzpicture}%
}

\begin{document}

\begin{titlepage}
\thispagestyle{empty}
\centering
\vspace*{3cm}
{\Huge\bfseries Bayesian SC vs Bayesian SDID\\\bigskip
\LARGE Geo-Lift Comparison on Simulated Panel
\par}
\vspace{1.5cm}
{\Large Magnus Foldeström\\ June 18, 2026\par}
\vfill
\end{titlepage}

\section{What this project does}

Inspired by a real-world migration in media-measurement platforms from
Bayesian Synthetic Control (BSC) toward Bayesian Synthetic
Difference-in-Differences (BSDID), this project fits both models on
the same simulated panel with a known campaign lift and reports the
difference. A representative single-panel result is shown for
intuition, followed by a robustness check across 20 simulated panels
that isolates systematic estimation error from sampling variability.

\section{Data}

A 51-unit, 80-week panel. One treated unit, 50 controls. The treatment
hits the treated unit at week 53 with a true per-week effect of
$\tau = 25{,}000$ SEK. Each unit has its own baseline, a shared trend,
annual seasonality, and Gaussian noise. Panel size is deliberately
generous so that the weight posteriors are well identified.

\section{Model specification}

\subsection{Notation}

Let $Y^{(0)}_t$ denote the treated unit's outcome at week $t$,
$Y^{(C)}_{i, t}$ the $i$-th control unit's outcome, $T_0$ the last
pre-treatment week and $T$ the last observed week. Let
$\omega \in \Delta^{N-1}$ (the $N$-simplex) be unit weights and
$\lambda \in \Delta^{T_0 - 1}$ time weights.

\subsection{Bayesian Synthetic Control (BSC)}

The treated unit's pre-period is modelled as a weighted average of
control units:
$$Y^{(0)}_t \sim \mathcal{N}\!\left(\sum_{i=1}^{N} \omega_i \, Y^{(C)}_{i,t},\ \sigma^2\right), \quad t = 1, \ldots, T_0$$

\textbf{Priors:}
$$\omega \sim \mathrm{Dirichlet}(\mathbf{1}_N), \quad \sigma \sim \mathcal{N}^{+}(0,\, \widehat{\sigma}_{\text{emp}}^{\,2})$$
where $\widehat{\sigma}_{\text{emp}} = \mathrm{sd}(Y^{(0)}_{1:T_0})$ is the empirical pre-period standard deviation of the treated unit.

\textbf{ATT (generated quantity):} For each posterior draw $s$,
$$\text{counterfactual}_t^{(s)} = \sum_i \omega_i^{(s)} \, Y^{(C)}_{i, t}, \quad \mathrm{ATT}_t^{(s)} = Y^{(0)}_t - \text{counterfactual}_t^{(s)}$$

for $t > T_0$. The reported point estimate is
$\overline{\mathrm{ATT}} = \tfrac{1}{T-T_0} \sum_{t > T_0} \mathrm{ATT}_t$ averaged over draws.

\textbf{Posterior:}
$$p(\omega, \sigma \mid Y^{(0)}, Y^{(C)}) \; \propto \; \mathrm{Dirichlet}(\omega \mid \mathbf{1}) \cdot \mathcal{N}^{+}(\sigma) \; \prod_{t=1}^{T_0} \mathcal{N}\!\left(Y^{(0)}_t \; \Big| \; \sum_i \omega_i Y^{(C)}_{i,t}, \sigma^2\right)$$

\subsection{Bayesian Synthetic Difference-in-Differences (BSDID)}

Same $\omega$-weighted control layer as BSC, plus a second simplex
$\lambda$ of pre-period time weights. The ATT subtracts the
$\lambda$-weighted pre-period gap:
$$\mathrm{ATT}_t = \Big(Y^{(0)}_t - \sum_i \omega_i Y^{(C)}_{i,t}\Big) - \sum_{s=1}^{T_0} \lambda_s \Big(Y^{(0)}_s - \sum_i \omega_i Y^{(C)}_{i,s}\Big)$$

Two likelihoods identify $\omega$ and $\lambda$ jointly:
$$\text{Likelihood 1 (treated pre):} \quad Y^{(0)}_t \sim \mathcal{N}\!\left(\sum_i \omega_i Y^{(C)}_{i, t}, \sigma^2\right), \quad t = 1, \ldots, T_0$$
$$\text{Likelihood 2 (controls' pre-vs-post):} \quad \overline{Y^{(C)}_{i, \text{post}}} \sim \mathcal{N}\!\left(\sum_t \lambda_t \, Y^{(C)}_{i, t},\ \sigma_\lambda^2\right), \quad i = 1, \ldots, N$$
where $\overline{Y^{(C)}_{i,\text{post}}} = \tfrac{1}{T - T_0} \sum_{t > T_0} Y^{(C)}_{i, t}$.

\textbf{Priors:}
$$\omega \sim \mathrm{Dirichlet}(\mathbf{1}_N), \quad \lambda \sim \mathrm{Dirichlet}(\mathbf{1}_{T_0})$$
$$\sigma \sim \mathcal{N}^{+}(0,\, \widehat{\sigma}_{\text{emp}}^{\,2}), \quad \sigma_\lambda \sim \mathcal{N}^{+}(0,\, \widehat{\sigma}_{\lambda,\text{emp}}^{\,2})$$
where $\widehat{\sigma}_{\lambda, \text{emp}}$ is the empirical standard deviation of
$\overline{Y^{(C)}_{i, \text{post}}}$ across controls.

\textbf{Posterior:}
$$p(\omega, \lambda, \sigma, \sigma_\lambda \mid Y^{(0)}, Y^{(C)}) \; \propto \; p(\omega) p(\lambda) p(\sigma) p(\sigma_\lambda) \cdot L_1 \cdot L_2$$
with $L_1$ and $L_2$ the two likelihoods above.

\section{Inference}

Both models fitted with Stan via cmdstanpy using HMC-NUTS. Diagnostics
below.

\begin{table}[H]
\centering
\begin{tabular}{l r}
\toprule
\textbf{Metric} & \textbf{Value} \\
\midrule
Chains              & 4 \\
Warmup iterations   & 1\,500 per chain \\
Sampling iterations & 1\,500 per chain \\
Post-warmup draws   & 6\,000 \\
$\max \widehat{R}$  & $\approx 1.00$ \\
Divergences         & 0 \\
\bottomrule
\end{tabular}
\caption{Convergence diagnostics for both BSC and BSDID fits.}
\end{table}

\section{Results}

\begin{table}[H]
\centering
\begin{tabular}{l r r r}
\toprule
 & \textbf{Truth} & \textbf{BSC} & \textbf{BSDID} \\
\midrule
Mean ATT       & 25\,000 & 23\,362 & \textbf{24\,913} \\
90\% interval  & --      & [19\,676,\ 27\,055] & [21\,473,\ 28\,304] \\
Interval width & --      & 7\,380  & \textbf{6\,831} \\
$|$Error$|$    & --      & 1\,638  & \textbf{87} \\
Covers truth   & --      & yes    & yes \\
\bottomrule
\end{tabular}
\caption{ATT posterior summary on the representative simulated panel (seed 19). BSDID's point estimate is essentially spot on (estimation error 87 SEK), against BSC's 1{,}638 SEK undershoot. BSDID also has a 7\,\% tighter 90\,\% credible interval. Both models' intervals contain the truth. The next section reports a robustness check across 20 panels.}
\end{table}

\subsection{Robustness check across 20 scenarios}

We repeat the full pipeline on 20 different simulated panels to
verify the advantage is not a single-seed artefact.

\begin{table}[H]
\centering
\begin{tabular}{l r r}
\toprule
\textbf{Metric (average across 20 panels)} & \textbf{BSC} & \textbf{BSDID} \\
\midrule
Mean absolute estimation error & 2\,942  & \textbf{2\,375} \\
Mean 90\,\% credible interval width & 7\,013  & \textbf{6\,363} \\
\bottomrule
\end{tabular}
\caption{BSDID delivers 19\,\% lower average estimation error and a
9\,\% tighter credible interval across the 20 simulated panels.}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{01_att_posteriors.png}
\caption{ATT posteriors. Dashed line is truth.}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{06_att_path.png}
\caption{Per-week ATT, posterior mean and 90\,\% CI.}
\end{figure}

\section{Why BSDID can outperform BSC}

BSC is exposed to systematic pre-period offset between the treated
market and its synthetic control. Any persistent gap in the
pre-campaign period leaks into the post-campaign lift estimate,
because BSC has no mechanism to remove it. BSDID's lambda
time-weights subtract the pre-campaign gap before computing the
lift, so systematic pre-period misalignment is netted out.

On the representative panel (seed 19), BSC undershoots the true lift
by 1{,}638 SEK per week, while BSDID lands within 87 SEK of truth.
Across 20 panels, BSDID's average absolute estimation error is
19\,\% lower and its credible interval 9\,\% tighter on average.

This is consistent with the motivation behind Arkhangelsky et al.'s
SDID framework. The advantage matters most whenever the treated
market sits above or below the convex hull of its controls in the
pre-campaign period, which is the common case on real geo data.

In this project the observed advantage is deliberately modest because
the simulator draws every unit's baseline from the same distribution
and gives them identical trends and seasonality. The treated market
therefore sits well inside the convex hull of its controls, and BSC
has little systematic offset to leak into the lift estimate. On real
data where the treated market has a distinct trend, scale, or
consumer profile relative to the control set, BSDID's improvement
over BSC is expected to be significantly larger.

\section{Implications for client engagements}

A few practical takeaways for geo-lift work with brand clients.

\textbf{Method choice:}

\begin{itemize}
  \item \textbf{Use BSDID when the treated market has a different
        trend or a persistent gap from its controls.} BSC can read
        that gap as campaign impact and overstate the lift. BSDID
        removes the gap first.
  \item \textbf{How much BSDID beats BSC depends on how different
        the treated market is.} If the treated market looks like a
        weighted mix of its controls (as in this simulation), the
        two methods land close together. If the treated market has
        its own trend, size, or audience mix, BSDID pulls further
        ahead.
  \item \textbf{BSDID's credible interval is on average 9\,\%
        tighter here.} That means a slightly smaller reported range
        when sharing lift numbers with clients.
  \item \textbf{Budget for a robustness check.} One fit tells you
        what the campaign looks like. Re-fitting on resampled or
        placebo panels tells you how much you can trust that answer.
\end{itemize}

\textbf{Client conversation:}

\begin{itemize}
  \item \textbf{Report lift as a range, not a single number.} "+12\,\%
        $\pm$ 3\,pp" is more honest than a single "+12\,\%" and
        avoids overpromising.
  \item \textbf{Use the method choice to explain the rigour.}
        Saying "we chose BSDID because it protects against a
        specific type of measurement error" gives the client a
        concrete reason to trust the number.
  \item \textbf{Show the per-week lift plot when walking through
        results.} It shows whether the effect ramped up, stayed
        flat, or faded — clients see the story, not just a single
        headline.
  \item \textbf{Turn the credible interval into a planning range.}
        "Lift is between +8\,\% and +16\,\%" gives a media planner
        two budget scenarios to work with. Easier to act on than a
        point estimate.
  \item \textbf{An honest lift report opens the next conversation.}
        Clients asking "how do we tighten the range next time?" is a
        natural bridge to a bigger panel, longer pre-period, or
        priors from earlier campaigns.
  \item \textbf{Robustness data helps with procurement questions.}
        If a client asks "how do you know this works?", you can point
        to how the method holds up across 20 simulated scenarios.
\end{itemize}

\section{Limitations}

This is a controlled method-comparison experiment where the ground
truth must be known exactly, so several simplifications are
deliberate: constant treatment effect, iid Gaussian noise,
homogeneous control regions, and a generative structure close to what
the models assume. The synthetic setup is cleaner than real geo data,
which has non-stationarity, structural breaks, autocorrelated noise,
regional shocks, and heteroskedasticity. Absolute numbers (BSDID
19\,\% lower mean absolute bias, 9\,\% tighter interval on the
20-seed Monte Carlo) reflect this synthetic setup and would move on
real data.

A 20-seed Monte Carlo study is included. Empirical coverage at the
nominal 90\,\% level is 75\,\% for both models, indicating slight
residual overconfidence. A production-grade implementation would add
informative priors on omega and lambda (e.g., tied to geographic
similarity or historical experiments), hierarchical pooling across
past lift tests, and a larger Monte Carlo (100+ seeds) to sharpen
the coverage estimate. Priors are currently uniform Dirichlet on both
weight simplices and treatment is pure on/off.

BSDID's theoretical advantage over BSC is that it subtracts a
systematic pre-period offset between treated and synthetic control
before computing the ATT. That advantage is most pronounced when the
treated unit has a differential trend, structural drift, or
persistent offset that BSC would misread as post-period treatment
effect. The synthetic panel here has homogeneous trends across units
and only a small pre-period offset on the treated unit, so BSDID's
correction has relatively little to correct. On real geo data with
differential regional trends and macro drift, BSDID's margin over
BSC is expected to widen.

\end{document}
